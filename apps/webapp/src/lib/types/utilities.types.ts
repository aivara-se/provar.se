/**
 * An interface for a date range.
 */
export interface DateRange {
	from: Date;
	to: Date;
}

/**
 * An interface for a stringified date range.
 */
export interface FormattedDateRange {
	from: string;
	to: string;
}
